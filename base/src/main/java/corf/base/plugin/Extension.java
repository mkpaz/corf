package corf.base.plugin;

/**
 * Extension points are "bricks" that are used to extend core application functionality.
 * Each plugin can provide one or more extension points of various types, but for now
 * only {@link Tool} type is supported by the application.
 */
public interface Extension {}
